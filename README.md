# Hide youtube thumbnails

1. Clone this repo
2. Navigate to the root directory
3. Run `npm install`
4. Run `npm run build`
5. Go to Chrome -> Manage Extenstions -> Load unpacked -> Select the root directory